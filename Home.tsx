import { LayoutTemplate, Video, Palette, FolderOpen, Send, ArrowRight } from 'lucide-react'; import { useStudioStore } from '../store/useStudioStore';
const tools=[['templates','Cricket Templates','100 cricket designs',LayoutTemplate],['editor','Poster Editor','Drag layers, edit text and export',LayoutTemplate],['video','Video Studio','Import, trim and export clips',Video],['brand','Brand Kit','Save your colors and logo',Palette],['projects','Projects','Open your saved posters',FolderOpen],['publish','Export & Share','Save a poster or share a file',Send]] as const;
export function Home(){const go=useStudioStore(s=>s.setRoute);return <div className="page homePage"><section className="hero"><div><span className="kicker">MK97 CREATIVE STUDIO</span><h1>Make the moment unforgettable.</h1><p>Build match day graphics, wicket announcements, MVP cards and short cricket clips on your phone.</p><div className="heroButtons"><button className="primary" onClick={()=>go('templates')}>Explore templates <ArrowRight size={18}/></button><button onClick={()=>go('editor')}>Continue editing</button></div></div><div className="heroArt"><img src="assets/mk97-logo.png" alt="MK97"/><div className="glow"/></div></section><section><div className="sectionHead"><div><span>WORKSPACES</span><h2>Your creative toolkit</h2></div></div><div className="toolCards">{tools.map(([r,t,d,I])=><button key={r} onClick={()=>go(r)}><I/><div><b>{t}</b><small>{d}</small></div><ArrowRight size={16}/></button>)}</div></section></div>}
